#!/bin/bash
# Minimal helper used by CI to decide build steps.
# Restored as a no-op to avoid build failures after temporary script removals.
set -e
echo "scripts/should-build-frontend.sh: no-op (restored) - OK"
exit 0


